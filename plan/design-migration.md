# 設計遷移規劃：backup 版本到模組化版本

## 執行日期
2025-11-16

## 目標
將 `index.html.backup` 中的品牌設計風格與功能改進套用到當前模組化版本 `index.html` 及相關 CSS/JS 元件。

---

## 一、設計差異分析

### 1.1 視覺設計變更

#### **色彩系統**
- **Backup 版本**：採用自然、沉穩的大地色調
  - 主色：深綠 `#2d3e2d`、`#3d4f3d`
  - 輔色：米色 `#f5ebe0`、咖啡 `#8b7355`
  - 背景：米白 `#fffaf5`、淺米 `#e8dcc8`
  - 使用大量漸層與半透明效果

- **當前版本**：使用明亮的暖色調
  - 主色：橘黃漸層 `#f6d365` → `#fda085`
  - 整體氛圍較為活潑

**結論**：backup 版本色調更符合「植物/自然」主題，建議採用。

#### **字體系統**
- **Backup 版本**：
  - 中文：`'Noto Serif TC'`（襯線體）
  - 英文：`'Cormorant Garamond'`（優雅襯線體）
  - 從 Google Fonts 載入

- **當前版本**：
  - 使用系統預設字體

**結論**：backup 版本字體更具品牌質感，建議採用。

#### **佈局設計**
- **Backup 版本**：
  - 新增 `brand-header`：包含品牌 LOGO「NUVAYAE SCENT LAB」
  - 新增 `brand-footer`：版權資訊
  - 卡片式容器有更豐富的陰影與邊框效果
  - 使用 `backdrop-filter: blur()` 實現毛玻璃效果

- **當前版本**：
  - 簡單的 header/content 結構
  - 無品牌識別元素

**結論**：backup 版本有更完整的品牌識別，建議採用。

---

### 1.2 功能性變更

#### **分享功能**
- **Backup 版本**：
  - 包含 `downloadResultImage()` 函式
  - 包含完整的 IG 分享卡片樣式（`.ig-share-card`）
  - 卡片尺寸：1080×1600px（符合 IG 限時動態尺寸）
  - 使用 `position: fixed; top: -10000px` 隱藏卡片

- **當前版本**：
  - 已有 `ShareManager` 模組（`js/share.js`）
  - 已引入 `html2canvas` 函式庫
  - 有分享按鈕與 `share-card.css`

**結論**：當前版本已有分享功能架構，需比對 CSS 樣式差異並整合。

#### **測驗題目文字**
- **Backup 版本**：開始畫面文字較簡潔
  ```
  透過 10 個生活情境問題,
  找到屬於你的情緒座標與植物人格。

  測驗結束後,你會獲得:
  ✨ 專屬療癒香氣能量
  🌱 適合與你一起生活的植物們
  ```

- **當前版本**：開始畫面文字較長
  ```
  想像你是一株植物。
  有習慣的氣候、適合生長的溫度。

  透過 10 個生活情境問題，
  我們會幫你找到現在的情緒座標，
  還有療癒能量，以及適合與你一起生活的植物們！
  ```

**結論**：兩者各有優點，可保留當前版本或提供選項給使用者選擇。

#### **座標軸標籤**
- **Backup 版本**：
  - 上方：`Energy`
  - 下方：`Temperature`

- **當前版本**：
  - 上方：`Temperature`
  - 左方：`Energy`

**結論**：backup 版本標籤位置較不符合邏輯（Energy 應在左側），當前版本較佳。

---

## 二、遷移計畫

### 2.1 檔案層級變更

#### **需要修改的檔案**
1. `index.html` - 新增品牌 header/footer、字體引入
2. `css/style.css` - 整合 backup 版本的色彩系統、字體、樣式
3. `css/share-card.css` - 比對 backup 版本的 IG 分享卡片樣式
4. `js/share.js` - 確認功能與 backup 版本一致

#### **不需修改的檔案**
- `js/data/questions.js` - 題目資料維持現有版本
- `js/data/plants.js` - 植物資料維持現有版本
- `js/quiz.js` - 邏輯無需變更
- `js/ui.js` - 邏輯無需變更，但可能需調整 DOM 選擇器

---

### 2.2 具體修改步驟

#### **步驟 1：index.html 結構調整**

**1.1 新增 Google Fonts 引入**
在 `<head>` 中新增：
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600&family=Noto+Serif+TC:wght@400;600&display=swap" rel="stylesheet">
```

**1.2 修改 container 結構**
```html
<div class="container">
    <!-- 新增品牌 header -->
    <div class="brand-header">
        <div class="brand-logo">NUVAYAE SCENT LAB</div>
        <h1>🌿 找到你的情緒座標</h1>
        <p>來自台灣的香氣研製所</p>
    </div>

    <div class="content">
        <!-- 現有內容維持不變 -->
    </div>

    <!-- 新增品牌 footer -->
    <div class="brand-footer">
        Nuvayae Scent Lab © 2025 | 來自台灣的香氣研製所
    </div>
</div>
```

**1.3 調整開始畫面文字**
選擇採用 backup 版本的簡潔文字。

---

#### **步驟 2：css/style.css 樣式整合**

**2.1 基礎樣式覆寫**

```css
/* 字體系統 */
body {
    font-family: 'Noto Serif TC', 'Cormorant Garamond', serif;
}

/* 背景色調 */
body {
    background: linear-gradient(135deg,
        rgba(45,62,45,0.85) 0%,
        rgba(74,95,74,0.88) 50%,
        rgba(61,79,61,0.85) 100%);
}

body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background:
        radial-gradient(circle at 20% 30%, rgba(255,248,240,0.08) 0%, transparent 50%),
        radial-gradient(circle at 80% 70%, rgba(139,115,85,0.06) 0%, transparent 50%);
    pointer-events: none;
}

/* 容器樣式 */
.container {
    background: linear-gradient(135deg,
        rgba(255,250,245,0.98) 0%,
        rgba(254,248,240,0.98) 100%);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(139,115,85,0.2);
    box-shadow:
        0 20px 60px rgba(0,0,0,0.3),
        inset 0 1px 0 rgba(255,255,255,0.6);
}
```

**2.2 新增品牌元素樣式**

```css
.brand-header {
    background: linear-gradient(135deg,
        rgba(45,62,45,0.95) 0%,
        rgba(61,79,61,0.95) 100%);
    backdrop-filter: blur(5px);
    color: #f5ebe0;
    padding: 32px 24px 28px;
    text-align: center;
    border-bottom: 2px solid #8b7355;
}

.brand-logo {
    font-size: 13px;
    letter-spacing: 3px;
    font-weight: 400;
    margin-bottom: 8px;
    color: #d4c5b0;
    font-family: 'Cormorant Garamond', serif;
}

.brand-footer {
    text-align: center;
    padding: 16px;
    background: rgba(255,248,240,0.5);
    border-top: 1px solid #e8dcc8;
    font-size: 12px;
    color: #8b7355;
    letter-spacing: 0.5px;
}

/* 響應式調整 */
@media (max-width: 520px) {
    .brand-header {
        padding: 24px 16px 20px;
    }
    .brand-header h1 {
        font-size: 24px;
    }
}
```

**2.3 更新色彩變數**

將所有按鈕、卡片、邊框色彩改為大地色系：
- 主按鈕：`#3d4f3d` → `#2d3e2d`
- 邊框色：`#d4c5b0`
- 文字色：標題 `#2d3e2d`、內文 `#5a4a3a`、輔助 `#8b7355`

---

#### **步驟 3：css/share-card.css 樣式比對**

**3.1 檢查現有 share-card.css**
需確認以下樣式是否完整：
- `.ig-share-card` 基礎容器（1080×1600px）
- `.ig-share-card__header` / `__footer`
- `.ig-share-card__coord-map` 座標圖樣式
- `.ig-share-card__relations` 關係卡片樣式
- `.ig-share-card__scents` 香氣推薦樣式

**3.2 整合 backup 版本樣式**
如發現缺失，從 backup 版本複製對應的 `.ig-share-card__*` 樣式。

**重點**：
- 確保所有動畫都被停用（`animation: none !important; transition: none !important;`）
- 確保卡片隱藏在畫面外（`position: fixed; top: -10000px; left: -10000px;`）

---

#### **步驟 4：js/share.js 功能驗證**

**4.1 確認 ShareManager 包含以下方法**
- `generateShareImage(resultElement, resultKey)` - 生成分享圖
- `copyLink()` - 複製連結

**4.2 確認分享卡片 DOM 生成邏輯**
- 是否動態生成 `.ig-share-card` 元素
- 是否正確填入植物資料、座標、關係、香氣資訊
- 是否使用 `html2canvas` 轉換為圖片

**4.3 測試流程**
1. 完成測驗並到達結果畫面
2. 點擊「分享結果圖」按鈕
3. 確認圖片正確生成並下載
4. 點擊「複製連結」按鈕
5. 確認連結已複製到剪貼簿

---

### 2.3 測試檢查清單

#### **視覺測試**
- [ ] 品牌 header 正確顯示（LOGO + 標題 + 副標題）
- [ ] 字體載入成功（Noto Serif TC + Cormorant Garamond）
- [ ] 色彩系統正確套用（大地色調）
- [ ] 毛玻璃效果正常（backdrop-filter）
- [ ] 陰影與邊框效果正確
- [ ] 品牌 footer 正確顯示

#### **功能測試**
- [ ] 測驗流程正常運作
- [ ] 結果畫面正確顯示
- [ ] 座標圖正確定位
- [ ] 關係卡片正確顯示（包含植物圖片）
- [ ] 香氣推薦正確顯示
- [ ] 分享圖片生成功能正常
- [ ] 複製連結功能正常

#### **響應式測試**
- [ ] 手機版（< 520px）佈局正常
- [ ] 桌面版（≥ 520px）佈局正常
- [ ] 品牌元素在不同螢幕尺寸下正確顯示

---

## 三、風險評估

### 3.1 潛在問題
1. **字體載入失敗**：Google Fonts 可能因網路問題無法載入
   - **解決方案**：提供 fallback 字體（系統預設襯線體）

2. **毛玻璃效果相容性**：`backdrop-filter` 在舊版瀏覽器不支援
   - **解決方案**：已有半透明背景作為 fallback

3. **分享卡片樣式衝突**：可能與現有 CSS 產生衝突
   - **解決方案**：使用 BEM 命名規範（`ig-share-card__*`）隔離樣式

4. **DOM 選擇器變更**：新增 brand-header 後可能影響現有 JS
   - **解決方案**：檢查 `ui.js` 中的選擇器是否依賴 `.header` 類別

### 3.2 回退計畫
如遷移後出現重大問題，可：
1. 回退到當前版本（透過 git）
2. 分階段部署（先測試字體與色彩，再測試品牌元素）


---

## 五、建議事項

1. **保留 backup 檔案**：在遷移完成前，保留 `index.html.backup` 作為參考
2. **建立 git commit**：在每個步驟完成後提交變更，方便回退
3. **先在本地測試**：確認所有功能正常後再部署到 GitHub Pages
4. **文件更新**：更新 `CLAUDE.md` 中的色彩系統與字體說明

---

## 六、使用者確認結果

✅ **已於 2025-11-16 確認**

1. **開始畫面文字**：
   - ✅ **採用選項 B（backup 版本）**：使用較簡潔的文字
   ```
   透過 10 個生活情境問題,
   找到屬於你的情緒座標與植物人格。

   測驗結束後,你會獲得:
   ✨ 專屬療癒香氣能量
   🌱 適合與你一起生活的植物們
   ```

2. **座標軸標籤位置**：
   - ✅ **採用選項 A（當前版本）**：上方 Temperature，左側 Energy
   - 注意：backup 版本的標籤位置需要調整

3. **品牌資訊**：
   - ✅ 品牌名稱「NUVAYAE SCENT LAB」拼寫正確
   - ✅ 副標題「來自台灣的香氣研製所」正確

---

## 七、執行順序（已確認，準備實作）

按以下順序執行：
1. ✅ 修改 `index.html` - 新增品牌元素、更新開始畫面文字、保持座標軸標籤
2. ✅ 修改 `css/style.css` - 整合 backup 版本的色彩系統、字體、樣式
3. ✅ 檢查並更新 `css/share-card.css` - 比對 backup 版本的 IG 分享卡片樣式
4. ✅ 檢查 `js/share.js` - 確認功能與 backup 版本一致
5. ✅ 執行測試檢查清單
6. ✅ 提交 git commit
7. ✅ 部署到 GitHub Pages

---

**規劃完成日期**：2025-11-16
**使用者確認日期**：2025-11-16
**準備開始實作** ✅
